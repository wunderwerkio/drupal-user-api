langcode: en
passwordless_login:
  subject: '[site:name] Login'
  body: |-
    ## Implement your email template here.
    ## For this to work you need a verification provider that integrates with the
    ## Verification API module that also supports tokens! (e.g. the magic_code module).
